name: 💣  1. Destroy a Infraestrutura (Terraform)

on:
  workflow_dispatch:
    inputs:
      terraform_action:
        description: 'Ação do Terraform (destroy)'
        required: true
        default: 'destroy'
        type: choice
        options:
          - destroy

jobs:
  terraform:
    name: 'Terraform ${{ github.event.inputs.terraform_action }}'
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ env.TF_WORKING_DIR }}

    steps:
        - name: Checkout Code
          uses: actions/checkout@v4

        - name: Configurar Credenciais AWS
          uses: aws-actions/configure-aws-credentials@v4
          with:
            aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
            aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
            aws-region: ${{ env.AWS_REGION }}

        - name: Configurar Terraform
          uses: hashicorp/setup-terraform@v3
          with:
            # Use a mesma versão que está no seu setup.yml
            terraform_version: 1.5.0 

        - name: Terraform Init
          id: init
          # Necessário para carregar o backend S3/DynamoDB e o estado
          run: terraform init

        - name: 💣 Executar Terraform Destroy
          id: destroy
          # O -auto-approve é necessário aqui, pois não há interação na Action
          run: terraform destroy -auto-approve

        - name: Notificar Sucesso
          run: echo "🎉 Todos os recursos gerenciados pelo Terraform foram destruídos com sucesso!"